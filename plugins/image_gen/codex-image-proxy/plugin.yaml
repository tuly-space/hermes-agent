name: codex-image-proxy
version: 1.0.0
description: "gpt-image-2 image generation via a local/remote hermes-codex-oauth-proxy (OpenAI-compatible /v1/images/generations). Does not use OPENAI_BASE_URL / OPENAI_API_KEY. Saves generated images to $HERMES_HOME/cache/images/."
author: NousResearch
kind: backend
